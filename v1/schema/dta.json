{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "dta",
    "type": "object",
    "properties": {
      "DistributorRegistered": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a new distributor has been registered."
      },
      "DistributorRequestCanceled": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a distributor request has been canceled."
      },
      "DistributorRequestProcessed": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a distributor request has been processed."
      },
      "DistributorRequestProcessing": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a distributor request is being processed."
      },
      "FundAdminRegistered": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a fund admin has been registered."
      },
      "FundTokenAllowlistUpdated": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a fund token allowlist has been updated."
      },
      "FundTokenRegistered": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a fund token has been registered."
      },
      "Initialized": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when the contract has been initialized."
      },
      "InvalidDTARequestSettlement": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when an invalid DTARequestSettlement contract is detected."
      },
      "MessageFailed": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a CCIP message has failed."
      },
      "NativeFundsRecovered": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when native funds have been recovered."
      },
      "OwnershipTransferred": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when the ownership has been transferred."
      },
      "RedemptionRequested": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a redemption has been requested."
      },
      "SubscriptionRequested": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a subscription has been requested."
      },
      "CCIPMessageRecvFailed": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a CCIP message receive has failed."
      },
      "DTAAdded": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a DTA has been added to the wallet."
      },
      "DTARemoved": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a DTA has been removed from the wallet."
      },
      "DTASettlementClosed": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a DTA settlement has been closed."
      },
      "DTASettlementOpened": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a DTA settlement has been opened."
      },
      "EmptyRequestType": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when an empty request type is encountered."
      },
      "InsufficientPaymentTokenBalance": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when there is insufficient payment token balance."
      },
      "InvalidSubscriptionCrossChainPayment": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when an invalid subscription cross chain payment is detected."
      },
      "SettlementFailed": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when a settlement has failed."
      },
      "TokenWithdrawn": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when tokens have been withdrawn."
      },
      "UnauthorizedSenderDTA": {
        "$ref": "#/$defs/verifiableEvent",
        "description": "Emitted when an unauthorized sender attempts to access DTA."
      }
    },
    "$defs": {
      "verifiableEvent": {
        "type": "object",
        "properties": {
          "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "The timestamp when the event was created"
          },
          "event": {
            "$ref": "#/$defs/eventInfo"
          },
          "metadata": {
            "$ref": "#/$defs/metadata"
          },
          "transaction": {
            "$ref": "#/$defs/transaction"
          }
        },
        "required": [
          "createdAt",
          "event",
          "metadata",
          "transaction"
        ]
      },
      "eventInfo": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "description": "The service/type of the event"
          },
          "name": {
            "type": "string",
            "description": "The name of the event"
          },
          "address": {
            "type": "string",
            "description": "The address of the contract that emitted the event"
          },
          "requestId": {
            "type": "string",
            "description": "The request identifier associated with the event"
          },
          "topicHash": {
            "type": "string",
            "description": "The topic hash of the event"
          }
        },
        "required": [
          "type",
          "name",
          "address",
          "requestId",
          "topicHash"
        ]
      },
      "transaction": {
        "type": "object",
        "properties": {
          "timestamp": {
            "type": "integer",
            "description": "The unix timestamp of the transaction"
          },
          "chainId": {
            "type": "string",
            "description": "The chain ID for the transaction"
          },
          "hash": {
            "type": "string",
            "description": "The hash of the transaction"
          }
        },
        "required": [
          "timestamp",
          "chainId",
          "hash"
        ]
      },
      "metadata": {
        "type": "object",
        "properties": {
          "chainId": {
            "type": "string",
            "description": "Chain ID"
          },
          "network": {
            "type": "string",
            "description": "Network name"
          },
          "workflowEvent": {
            "$ref": "#/$defs/workflowEvent"
          }
        },
        "required": [
          "chainId"
        ]
      },
      "workflowEvent": {
        "type": "object",
        "properties": {
          "attributes": {
            "type": "object",
            "additionalProperties": { "$ref": "#/$defs/attribute" },
            "description": "A map of attribute key to attribute details"
          },
          "business_event_id": { "type": "string" },
          "component": { "type": "string" },
          "event_timestamp": { "type": "string", "format": "date-time" },
          "event_type_label": { "type": "string" },
          "failed": { "type": "boolean" },
          "final_event": { "type": "boolean" },
          "id": { "type": "string" },
          "participant": { "type": "string" },
          "participant_role": { "type": "string" },
          "process_labels": { "type": "array", "items": { "type": "string" } },
          "raw_data": { "type": "string" },
          "title": { "type": "string" }
        }
      },
      "attribute": {
        "type": "object",
        "properties": {
          "key": { "type": "string" },
          "on_chain": { "type": "boolean" },
          "value": { "type": "string" },
          "visibility": { "type": "string" }
        },
        "required": ["key", "on_chain", "value", "visibility"]
      }
    }
  }